// "Tu estilo en tus palabras" — límite compartido entre el card (maxLength),
// la server action (slice) y el slice defensivo de los motores. Una sola
// fuente: si cambia, cambia en todos lados a la vez.
export const STYLE_WORDS_MAX = 280;
