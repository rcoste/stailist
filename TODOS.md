# TODOS — stailist

Trabajo diferido con contexto. Cada ítem tiene su "por qué ahora no" y su trigger de reapertura.

## Corpus de prendas + aprobación a biblioteca (2026-08-17)

- **Qué**: cada render de prenda que nace se copia a un bucket compartido y
  entra como candidata `pendiente` (`library_candidates` extendida); pantalla
  de aprobación en /admin con señal de duplicado; al aprobar nace el arquetipo
  en la biblioteca; al borrar/resetear una cuenta las candidatas se desligan
  (`user_id = null`), no se borran. Spec completa:
  `docs/designs/corpus-de-prendas.md`.
- **Por qué ahora no**: toca base + pipeline de renders → va con ship
  completo (eng review), y el paso previo es sembrar a biblioteca los ~27
  renders curados del archivo de las cuentas de prueba de Roberto.
- **Trigger**: al terminar la siembra manual, o cuando el siguiente usuario
  real borre/resetee su cuenta (ahí se pierde dato de verdad).

## Huecos del corpus que dejó la pestaña de Trajes (2026-08-17)

- [x] ~~**Falta un traje NEGRO**~~ — HECHO (migración 0139, v0.2.250.1). Saco + pantalón negros de hombre; el onboarding de hombre queda con los tres que pidió Roberto: marino, gris carbón y negro.
- [x] ~~**Ninguna mujer tiene traje**~~ — HECHO (migración 0139, v0.2.250.1). Dos trajes sastre de mujer (negro y marino) con corte femenino descrito pieza por pieza —solapa angosta, cintura con pinzas, hombro estrecho, tiro alto— y revisados uno por uno: "que no parezca traje de hombre sobre mujer". La pestaña Trajes ya le aparece a las usuarias. Sigue abierto el hueco mayor del audit de género (la biblioteca formal completa, 22H/7M).
- [ ] **El traje entra completo o no entra**: la tarjeta marca las dos piezas de un tap y no hay "sólo el saco". Quien tenga el saco y no el pantalón tiene que borrar el pantalón después desde el clóset. Se dejó así porque un segundo control en la tarjeta paga complejidad en el minuto más caro del onboarding. *Trigger*: si alguien reporta que le entró una prenda que no tiene.

## Diferido del header de pantalla interna (2026-08-14)

- [ ] **¿El detalle de viaje adopta el header interno?**: el handoff de diseño (`claude-design-handoffs-stailist/design_handoff_esenciales`) lo declara PATRÓN GLOBAL "incluido el detalle de viaje", y se aplicó sólo a esenciales y biblioteca. *Por qué viaje quedó fuera*: tiene un header de DOS capas que ya funciona —la portada con la foto del destino y el back circular flotando encima, más una capa compacta al hacer scroll— y ese diseño existe PORQUE hay portada; el propio handoff dice "SIN portada de imagen, a diferencia de viaje" dos líneas antes de meterlo en el mismo saco. Rediseñar un header que se shippeó hace días y resuelve otro caso es riesgo sin retorno. *Trigger*: cuando se toque el detalle de viaje por otra razón, evaluar si la capa compacta (la que ya enseña "‹ modo viaje" + "···") puede ser directamente el header interno.
- [ ] **Biblioteca en desktop se quedó sin back propio**: su link "‹ clóset" no llevaba `lg:hidden` y ahora vive en el header móvil, que sí lo lleva. En desktop el destino sigue a un clic (el nav persistente tiene "Clóset"), así que no hay callejón — pero es un grado menos de explícito que antes. *Trigger*: si alguien reporta que en desktop no encuentra la vuelta.

## Diferido de la observabilidad de IA (2026-08-14, v0.2.238.0)

- [ ] **¿Flash-Lite es el modelo correcto para JUZGAR, o sólo para leer?** Los dos jueces que miran —la rúbrica de visión (rv3) y el juez stylist (js1)— corren en `VISION_MODEL` (Gemini 3.1 Flash-Lite). Ese modelo ganó una prueba a ciegas de **leer prendas** (extraer color, tipo y material: 0.80 errores/foto contra once modelos, 27× más barato que Opus), y criticar estilo es tarea más difícil que extraer. El dato que lo hace sospechoso: la rúbrica de visión coincide con Roberto el **84%**, por debajo del **87%** que sacaría aprobar todo, y caza sólo **2 de sus 8 👎** (el juez de texto caza 4). Puede que no sea que "la visión no aporta" sino un modelo lite en una tarea que le queda grande. **Decisión de Roberto (2026-08-18): no ahorita** — no bloquea nada y hay cosas más urgentes. *Cómo se probaría*: el modo visión de `/admin/comparador`, que existe justo para esto y costó $0.48 la corrida entera. *Trigger*: que el juez stylist entregue hallazgos flojos en su primera corrida, o antes de apoyar cualquier decisión del motor en lo que dicen los jueces que miran.

- [ ] **Tarifa por imagen + medir `lib/gemini-imagen.ts`** (EL HUECO MÁS CARO): un archivo, siete consumidores — try-on, avatar, arquetipos, renders de prenda y fotos de destino pasan todos por ahí y ninguno deja recibo. No se olvidó: una imagen no se cobra por token y `lib/proveedores/precios.ts` sólo sabe de tokens, así que hace falta una tarifa por imagen ANTES de poder instrumentarlo. Con el gasto de imágenes sin medir, el total de `/admin/ia` es un piso, no el costo real. *Trigger*: el siguiente susto de factura, o antes de cualquier decisión de "¿nos conviene generar menos imágenes?".
- [ ] **Migrar los 13 caminos con SDK directo a la puerta común**: están declarados uno por uno en `lib/cobertura-recibos.ts` (lo lee el panel para confesar sus huecos) y en `EXENTOS` de `lib/cobertura-recibos.test.ts` (lo exige el candado); un test obliga a que las dos listas coincidan, así que migrar uno pide borrarlo de ambas. El más valioso es `lib/engine/capsule-target.ts` — armar esenciales es la llamada más larga del producto (~40s de Opus, tope de 300s) y es justo la que no se puede medir. También el modo Viaje entero (`trip-outfits`, `trip-substitutes`, `itinerario`) y la segunda pasada del avatar. *Por qué ahora no*: cada uno tiene su propio manejo de streaming, reintentos y parseo; migrarlos en bloque el mismo día que nace la instrumentación mezcla dos riesgos. *Trigger*: al tocar cualquiera de ellos por otra razón, migrarlo de paso.
- [ ] **Lo que el candado NO puede ver**: que la RUTA le pase el contexto a la función de `lib/`. El parámetro es opcional a propósito (el comparador, los evales y los scripts llaman lo mismo sin sesión), así que una ruta que se olvide compila, corre y no deja recibo — sin que nada truene. Blindado está el eslabón donde se perdía todo (que la llamada pase por `medir`), no el cableado. *Fix posible*: un test de rutas que verifique que cada `app/api/**` que llama a un módulo instrumentado le pasa `quien`. *Trigger*: la primera tarea que aparezca en `/admin/ia` con muchas menos llamadas de las esperadas.
- [ ] **Retención de `ai_calls`**: la tabla crece sin tope y el panel trae hasta 2000 filas por carga. Con el volumen real (decenas al día) da para meses, pero no hay borrado automático ni agregado histórico. *Trigger*: cuando el panel se sienta lento o la tabla pase de ~100k filas.

## Diferido de la review de ship (2026-08-13, v0.2.237.0)

- [ ] **Un solo motor de colas de render (`runQueue`)**: `capsule-list` tiene ahora DOS colas con la misma mecánica escritas a 25 líneas de distancia (lista derivada → ref → bandera → efecto con workers) — el prewarm de prendas ideales delega en `lib/prewarm-renders` con AbortController y tope, y la cola nueva de prendas propias trae sus workers inline con una bandera `vivo`. Ya divergieron en cómo cancelan. *Fix*: subir la mecánica a `runQueue(jobs, worker, {concurrency, signal})` junto a `prewarmRenders` y que ambas la usen. *Por qué ahora no*: las dos acaban de recibir el mismo fix de fondo (la firma que se autocancelaba) y unificarlas el mismo día mezcla el arreglo con el refactor. *Trigger*: la tercera cola, o el próximo cambio que toque cualquiera de las dos.
- [ ] **Candado silencioso compartido (`useLock` / `useLockPorId`)**: cuatro copias del mismo patrón `if (ref.current) return; ref.current = true; … finally { ref.current = false }` (favorite-button, skip-reasons, y los dos de espejo-flow). No es cosmético: dos de las cuatro nacieron SIN el `finally` y dejaban el botón muerto para siempre si la acción reventaba — se arreglaron en este ship, pero la quinta copia volverá a nacer torcida. *Ojo*: `yendo` (usuarios-table) y `abriendoUltimo` (hoy-client) NO entran — son estado de UI que además pinta, no candados. *Trigger*: el quinto candado.
- [ ] **`introVista(hints, id)` en un módulo puro**: `lib/intros.ts` es `"use server"` y ahí sólo se pueden exportar funciones async, así que dos páginas arman la llave `intro:<id>` a mano como string literal. Si el prefijo cambia, el gate deja de detectar la intro vista y nadie se entera hasta que la pantalla se repite. *Fix*: un módulo de dominio puro con `KEY` + `introVista`. *Por qué ahora no*: crear un archivo nuevo a mitad de un ship por un acoplamiento de string; el intento directo rompió el build (lo cazó la verificación en navegador, no los tests). *Trigger*: la tercera página que lea una intro.
- [ ] **Fusionar `loadClosetImageMap` y `loadClosetNameToId`**: son la misma consulta a `items` (mismos filtros, mismo join a `archetypes`, ambas traen el jsonb `attrs`) y desde este ship las dos corren en cada carga de `/closet/capsula`. *Fix*: agregar `id` al select de la primera y devolver los dos mapas de un solo recorrido — el bucle ya itera exactamente las mismas filas. *Trigger*: cuando la pantalla de esenciales se sienta lenta, o al tocar `lib/capsule-data.ts`.
- [ ] **No reintentar renders que ya fallaron**: `renderItemImage` marca `render_status='failed'`, pero la cola nueva de prendas propias no lo sabe (sólo recibe nombre→id), así que cada visita vuelve a pedir —y a pagar— el render de una prenda que nunca sale. *Fix*: pasar el `render_status` en el mapa y saltarse las fallidas. *Trigger*: la primera prenda que aparezca varias veces en los recibos de `ai_calls` sin llegar nunca a `done`.
- [ ] **Huecos de test de este ship** (cobertura 68%): los tres candados de `espejo-flow` (alcanzar el paso "dibujando" exige montar subida de foto + guardado + render), las rutas de error de `verUltimoLook` en `hoy-client` (pide extraerla a un helper puro), y qué crédito de intro gasta `esenciales-intro` según el modo. *Trigger*: el próximo cambio que toque cualquiera de los tres.

## Diferido de la review de ship (2026-08-13, viaje handoff 2)

- [ ] **Token de scrim de tinta (unificar 5 copias)**: la portada del viaje estrenó otro gradiente inline `rgb(20 20 20 / .74)` sobre foto, sumándose a espejo-flow (.72/.5), avatar-wizard (.34/.78) y colorimetria/quiz (.55) — cinco archivos, cada uno con su alpha de la misma tinta. *Por qué ahora no*: crear un token nuevo requiere tocar DESIGN.md + globals.css con OK de Roberto (regla del DS: no inventar tokens al vuelo), y el ship no es el lugar. *Trigger*: la próxima pantalla que necesite scrim — ahí se decide el/los alphas canónicos y se migran las cinco.
- [ ] **Señal de "maleta desactualizada" al editar la ruta sin rehacer**: el camino "solo guarda ruta y fechas" permite cambiar el destino entero (playa→montaña) conservando una cápsula que razona el viaje viejo; si aún no hay looks, no queda NINGUNA señal de desajuste (outfits_stale solo marca looks existentes). La hoja de decisión ya avisa ("otra ruta puede pedir otra ropa"), pero el detalle no lo recuerda después. *Trigger*: primer usuario real que edite lugares por el camino ligero; el fix natural es un aviso en "el plan"/"prendas" cuando `paradas` ya no coincide con las que generaron `capsule_target`.
- [ ] **Persistir las anclas del viaje**: "rehaz mi maleta" desde editar ruta regenera sin las prendas que la persona ancló ("llevar sí o sí") — no se guardan en `trips`, solo viven en la generación original. *Fix*: columna o llave en overrides al crear + prefill en modo edición + reaplicar en el rebuild. *Trigger*: primera queja de "¿dónde quedó mi prenda anclada?" tras un rehacer.
- [ ] **Race de overrides contra rebuild/candidatas en vuelo**: las actions de overrides hacen leer-modificar-escribir del blob completo; un rebuild por editId que aterriza mientras otra pestaña decide duelos puede re-escribir llaves `cand:i`/`sub:i` de la cápsula VIEJA sobre la nueva (los índices cambian de significado). Ventanas chicas, un solo usuario — improbable hoy. *Fix de fondo*: versionar la cápsula (llave `gen` en overrides) y validar versión en cada write, o `jsonb_set` atómico por llave. *Trigger*: primer reporte de decisión de duelo "fantasma" tras rehacer.
- [ ] **"Rehacer" looks hereda corazones/try-on por índice (pre-existente)**: el rebuild por editId ya desvincula `trip_look_index` (este ship), pero el "Rehacer" de la pestaña looks (POST outfits sin append) deja las filas viejas de `outfits` enganchadas por índice — un look nuevo puede heredar el corazón y el try-on de uno viejo. *Trigger*: al tocar la ruta de outfits; el fix es el mismo `trip_look_index = null` al reemplazar el set.

## Diferido del planning (2026-06-10)

- [x] **E2 — Detección multi-prenda en una foto** — **HECHO** (flujo "sube varias de golpe": `import-carrete-flow` + `/api/analizar-prendas`, migración 0045; completado 2026-07-23 en v0.2.13.0 con soporte flat-lay — ropa extendida en cama/ganchos/pila, tope 8 prendas/foto). Histórico de la decisión abajo; spec vigente: `docs/designs/import-carrete-multiprenda.md`. ~~("sube una foto tipo Instagram → saco tus prendas"): subir una foto con varias prendas (outfit puesto, ropa sobre la cama) → la IA propone N altas, cada una con confirmación editable.~~ *Por qué ahora no*: el alfa de Replit ya alucinaba analizando UNA prenda; multi-prenda es estrictamente más difícil (errores que se acumulan envenenan el motor), y el experimento no la necesita — el checklist de 15 básicos ya mató la fricción de catalogar. *Trigger*: hay usuarios reales Y señal de que el flujo actual de una-prenda-por-foto (`/closet`) se siente lento o quieren meter más ropa. Candidata #1 para el primer feature post-validación (es el upgrade natural del paso 8 del USER-JOURNEY, "el clóset crece").

  **→ SPEC COMPLETO: `docs/designs/import-carrete-multiprenda.md` (2026-06-20).** Ahí vive el diseño vigente, con corte MVP, criterios de aceptación y esfuerzo (~20h). El bloque de abajo (2026-06-16) queda como histórico; **fue superado en un punto**: el spec del 2026-06-20 SÍ genera una imagen limpia por prenda con Gemini (desde atributos confirmados, no recortando la foto cruda), en vez del "atributos-only / solo swatch" de 2026-06-16. Además agrega: dos confirmaciones (texto→render), y los renders rechazados no se borran — van a un staging de admin que decide si entran a la biblioteca general.

  **Diseño acordado (2026-06-16) — histórico, ver spec para lo vigente:**
  - **Principio que lo salva:** el clóset necesita ATRIBUTOS precisos (color hex, formalidad, tipo), no fotos bonitas — el motor combina por atributos, la imagen es secundaria.
  - **Visual de cada prenda:** NO recortar por prenda (bounding boxes poco confiables + recortes de ropa puesta se ven feos). Usar **swatch de color** (como el fallback de arquetipos) o foto compartida. Esto resuelve la vieja "decisión pendiente" (recorte vs foto compartida): se elige NO recortar.
  - **Pipeline:** foto → Claude visión con structured output → **array** de prendas `{tipo, color, color_hex, formalidad, temporada, nombre}`. Reusa la infra de `/api/analizar-prenda`, solo cambia el schema de 1 a N.
  - **Confirmación editable en lote (no negociable):** N tarjetas; cada una se puede apagar/editar/borrar. Nunca auto-agregar — el humano es el filtro contra la alucinación. Reusa el modal de confirmación actual vuelto lista.
  - **Fallback:** 0 prendas o baja confianza → cae al flujo de una-prenda-a-la-vez. Nunca bloquea.
  - **Tope:** ~6 prendas por foto. **Métrica:** % de prendas que el usuario deja sin editar (proxy de precisión) + abandono.
  - **Nota sobre la variante "outfit puesto":** es la MÁS difícil de leer (oclusión, perspectiva) aunque la más fácil de dar. El flat-lay extrae más limpio. Si se construye con foto de persona, comprometerse a atributos-only (sin recorte).
  - **Dónde vive:** segundo modo en `/closet` junto a "+ Foto"; opcional en onboarding DESPUÉS del checklist de 15 básicos (no en lugar de — el checklist es el piso de cero fricción).

## Diferido del build (sesión 2, 2026-06-14)

- [ ] **Captura unificada para colorimetría + avatar de try-on**: hoy son dos fotos distintas (selfie de cara para color, cuerpo completo para el avatar del try-on). Idea: pedir **una sola foto de cuerpo completo con la cara visible y buena luz** → recortar la cara para colorimetría + usar el cuadro completo como base del avatar. *Por qué ahora no*: el try-on ya está fuera del MVP (opcional, perezoso, no toca el camino crítico de los <2 min); meter captura de avatar en onboarding agrega fricción al flujo que estamos blindando. *Por qué NO derivar el avatar de la selfie de cara actual*: una foto de cara no se convierte en un cuerpo fiel — el modelo inventa proporciones que no son del usuario, y eso mata el "verme a mí". *Nota descartada*: complementar con estatura/peso/tipo de cuerpo NO mejora la imagen generada (los modelos de imagen no controlan geometría con números); esos datos sirven para lógica de fit/sizing, no para el avatar. *Trigger*: el try-on demuestra uso real y la doble subida de foto se vuelve fricción medible. *Tradeoff al reabrir*: cuerpo completo = cara más chica y luz menos controlada → la lectura de color baja; el quiz seguiría siendo la fuente de verdad del color.

## Diferido del build (2026-07-01)

- [x] **Backfill de atributos ricos (material / patrón / color secundario)** — HECHO 2026-07-01 (mismo día que v21): `scripts/backfill-atributos-ricos.mjs` re-infirió las 415 prendas activas con `claude-haiku-4-5` desde el nombre (validado vs Opus: patrón 12/12, material 11/12), escritura aditiva (jamás pisa datos existentes ni correcciones del usuario), todo marcado `atributos_v21: "backfill-texto-haiku"` en attrs. Resultado: 415 con patrón, 151 con material (solo donde el nombre daba señal — sin adivinar). La nota del red team también quedó: material/patrón/segundo color son editables en la hoja de la prenda (`updateItemAttrs` + closet-grid). *Pase de VISIÓN también hecho* (mismo día, con `SUPABASE_SERVICE_ROLE_KEY` local): 410/415 con material (validado vs Opus: patrón 8/8, material 7/8), 5 patrones corregidos por la foto. Restan 4 prendas sin imagen y 1 sin señal — irrelevantes.

## Diferido del comparador de motores (2026-08-05)

- [ ] **Nivel de certeza por prenda** (`confirmed_exact` / `confirmed_generic` / `assumed`): una prenda subida por foto es un dato duro; un básico del checklist es una aproximación ("tengo jeans" no dice si son rectos o wide). Hoy el motor las trata igual. Guardar la certeza permitiría (a) que el motor confíe distinto, (b) pedir la variante visual solo donde importa ("¿los tuyos son ajustados, rectos u holgados?"). *Origen*: la única idea genuinamente nueva del análisis de motores del 2026-08-05 (doc de ChatGPT); todo lo demás ya existía o ya había perdido su A/B. *Por qué ahora no*: es trabajo de clóset/onboarding, no del comparador, y el experimento con usuarias reales sigue pendiente. *Trigger*: defectos del comparador o quejas reales que se expliquen por "el motor asumió un corte que no era".

## Fases futuras (del design doc — NO tocar hasta validar el MVP)

- [ ] Sugerencias de compra estratégica con ROI ("esta pieza desbloquea 8 outfits") — el modelo de negocio latente.
- [ ] Modo maleta / empacador de viajes (la feature por la que Roberto pagaría; validada emocionalmente por Toño).
- [ ] Análisis de colorimetría por selfie (hoy: quiz manual — el análisis automático es frágil: iluminación, balance de blancos).
- [ ] Catálogos de marcas vía programas de afiliados o catálogo semilla curado (NUNCA scraping — viola ToS).
- [ ] Compartir outfit como imagen/link (E3 — descartada en ceremonia CEO 2026-06-10; reconsiderar solo si la calidad de outfits valida y hay demanda orgánica de compartir).
- [ ] Interfaz híbrida conversacional (chat + botones, sugerencia de Toño) — candidata para v2 del flujo de generación.
- [ ] Monetización freemium / referidos e-commerce (hipótesis de Toño: referidos > suscripción). Validar willingness-to-pay primero.
- [ ] **Panel admin de métricas**: hoy las métricas (TTV, ratio de votos, tiempos de generación) se consultan en el dashboard de Supabase. Un panel propio tiene sentido con 20+ usuarios. *Trigger*: la beta crece más allá del círculo cercano.

## Diferido del research competitivo (Aesty + gist de cutouts, 2026-07-13; Mira, 2026-07-14)

- [ ] **Screenshot-to-outfit** (inspirado en la feature estrella de Aesty): screenshotear un look de IG/TikTok → la IA identifica las prendas → "¿tienes algo así en tu clóset?" / wishlist. Encaja natural con el import-carrete (E2) y con la técnica del gist de tandpfun (reconstruir prenda por imagegen, no segmentar). *Por qué ahora no*: feature grande, cero usuarios validando el core. *Trigger*: E2 construido y usuarios pidiendo inspiración externa. *Dato*: es lo que Aesty monetiza con links de compra.
- [x] **Endurecer análisis por visión con `confianza` + `inseguro`** (del gist `extract-clothing-cutouts`) — HECHO 2026-07-13. `/api/analizar-prenda` devuelve `confianza` (alta/media/baja) + `inseguro` (campos que dudó); la confirmación editable (`add-photo-flow`) resalta esos campos (⚠️ + text-warning) y muestra aviso cálido. Verificado: nítida→alta/sin dudas, ambigua→media/marca material. FALTA (diferido): el multi-prenda (`import-carrete-flow` + `analizar-prendas`) puede adoptar el mismo patrón — su schema aún no trae los campos.
- [ ] **Paid marketing (AdWords) + decisiones de monetización**: Roberto quiere probar una campaña chica de adquisición pagada; de la mano viene decidir qué es gratis y qué no. *Contexto del research*: Aesty cobra $49.99–$79.99/año (tier Max $249.99/año, probablemente por try-on) y hay usuarias pagando contentas → hay willingness-to-pay en la categoría. *Objeción registrada (Claude)*: pagar tráfico antes de que UN usuario cercano complete el funnel es quemar dinero para descubrir bugs que Tatiana descubre gratis; orden sugerido: experimento con 2-3 cercanos → arreglar lo que truene → campaña chica con presupuesto tope. *Trigger*: experimento corrido o decisión explícita de Roberto de saltárselo. **→ Readiness técnico completo: `docs/designs/adwords-readiness.md` (4 bloqueadores, decisiones, orden de build).**
- [ ] **Try-on como anzuelo temprano de onboarding** (del research de Mira/trymira.style, 2026-07-14): Mira es un probador virtual por foto cuyo gancho es el "wow" instantáneo de verte con una prenda en 3s — TTV-al-wow brutal, más viral que "arma outfits con tu ropa". Nosotros YA tenemos el avatar try-on, pero vive enterrado dentro del look generado. Idea: usar ese momento visual impactante ANTES en el flujo (onboarding o primer contacto) como anzuelo, no como feature escondida. Conecta con el audit de IA (`docs/designs/audit-ia-flujos.md` — features escondidas). *Por qué ahora no*: el try-on está fuera del camino crítico de los <2 min y meterlo en onboarding agrega fricción; validar el core primero. *Trigger*: retención probada y búsqueda de un gancho de adquisición más fuerte (va de la mano con paid marketing).

  **Contexto competitivo Mira (para el registro):** trymira.style = probador virtual por foto (extensión Chrome + apps), dev indie sin funding/prensa/reseñas, ~$2.99/mes. **NO es competidor directo — es adyacente:** resuelve *pre-compra* ("¿me quedará esto que voy a comprar?"), nosotros *post-compra* ("¿qué me pongo con lo que ya tengo?"). No cataloga clóset (esquiva la fricción eligiendo otro job). Su producto es episódico (churn alto → por eso $2.99); el nuestro es de hábito diario. **Trampa a evitar:** NO meter "probar ropa de tiendas online" al MVP para igualarlos — es scope creep hacia su job y categoría comoditizada (docena de apps a 4.5★). Nuestro foso = clóset real + hábito diario en español.

## Diferido de la review de ship (2026-08-11, v0.2.222.0)

- [ ] **Sacar `DictadoSheet` de `weather-picker.tsx`**: el archivo llegó a ~2360 líneas y el subsistema de dictado (tipos de Web Speech, `ctorDeDictado` y el componente, ~250 líneas) es autocontenido — solo se acopla por el contrato `inicial/onUsar/onClose`. *Fix*: mover a `components/dictado-sheet.tsx`. *Por qué ahora no*: extraerlo el día del ship agrega churn al diff sin valor para nadie que use la app. *Trigger*: el próximo cambio que toque el dictado, o el siguiente que haga crecer el wizard.
- [ ] **Enseñar `weather.techado` en el historial**: hoy la marca se guarda pero NINGUNA pantalla la lee — sirve solo para diagnosticar consultando la tabla. Sin ella a la vista, un look de día lluvioso con mocasines sigue leyéndose como defecto del motor cuando fue lo que la persona pidió. *Fix*: mostrarla en el detalle del look (junto al clima) y en `/admin/evales`. *Trigger*: el primer "¿por qué me puso estos zapatos si llovía?".
- [ ] **Cobertura de las rutas de API**: `/api/look-of-day` y `/api/generate` no tienen ningún test (el repo nunca ha tenido arnés de rutas). La lógica pura ya se extrajo y sí está probada (`climaParaElMotor`), pero la mitad que vive inline en las rutas —la marca que se persiste y la supresión del paraguas, duplicada en las dos— no. *Fix*: extraer `climaParaGuardar(weather, techado)` a `lib/weather` y probarla, o montar un arnés de rutas con Supabase mockeado. *Trigger*: la próxima vez que las dos rutas tengan que estar de acuerdo en algo (ya derivaron antes).
- [ ] **El wow (`/api/generate`) ignora `seedItemId`** — ~~`formality` y `tipoEvento`~~ **resueltos en v0.2.223.2**: quien elige "una boda" en su primer look ya recibe un outfit que sabe que es una boda. Lo que sigue sin pasar es la prenda semilla: si alguien pide su primer look "con estos jeans", la ruta recibe el id y no lo usa. *Fix*: pasarlo a `construirContexto` como ya se pasan los otros dos. *Por qué ahora no*: `seedItemId` no lo produce ninguna pantalla todavía (el wizard no ofrece "empieza por esta prenda"), así que hoy no hay nadie a quien se le pierda. *Trigger*: cuando alguna pantalla empiece a mandarlo.

## Diferido de la review de ship (2026-07-22, v0.2.5.0)

- [ ] **Rate limit real (WAF) en `/api/permiso`**: el endpoint público del permiso parental lleva un limitador en memoria por instancia (10 req/min/IP) — suficiente para cortar loops de curl, pero no protege entre instancias serverless. *Fix*: regla de rate limit en el WAF de Vercel (o token bucket compartido en DB/KV). *Trigger*: abrir la beta más allá de la allowlist, o tráfico raro en los logs del endpoint.
- [ ] **HTML de marca standalone duplicado (a medias)**: los CORREOS ya se unificaron — el semanal y el reenganche de 48h comparten `lib/email-marca.ts` desde 2026-08-14, que nació justo con el disparador que decía este pendiente. Siguen fuera las PÁGINAS: el correo al tutor (`lib/consentimiento.ts`), la página de permiso (`app/api/permiso/route.ts`) y la baja de correo (`app/api/email/baja`) repiten font stacks, paleta hex y el wordmark carácter por carácter. *Por qué no ahora*: `email-marca` compone filas de `<table>` para clientes de correo; una página que se abre en un navegador quiere otra cosa, y forzarlas al mismo molde deja un módulo que no sirve bien a ninguna. *Fix*: extraer solo las CONSTANTES compartidas (font stacks, hex, wordmark) y dejar que cada medio arme su maqueta. *Trigger*: la próxima página standalone nueva, o un ajuste de marca.
- [ ] **Feedback visible cuando el reenvío del correo al tutor falla**: hoy el fallo de Postmark solo se loggea (server); la card de Perfil muestra "último correo enviado hace X" pero no distingue un envío fallido. *Fix*: `useFormState` en la card con el resultado del action. *Trigger*: primer reporte de "no le llegó el correo a mi papá".
- [ ] **Cableado perfil→motor sigue creciendo a mano** (ver también el DRY de v0.2.0.0 abajo): la señal de edad tocó 6 call sites con el mismo patrón select+cast. El helper `engineProfileSignals(profile)` mataría ambos TODOs juntos. *Trigger*: la próxima señal nueva.

## Diferido de la review de ship (2026-07-21, v0.2.0.0)

- [ ] **DRY de señales de estilo en los motores**: los builders de fragmentos de prompt (`palabrasTxt`, `refTxt`, `feedbackTxt`) están copiados en 4 motores (prompt.ts, capsule-target, trip-capsule, trip-outfits), y el cableado perfil→inputs (vetos + referencia + palabras + tasteSignal) en 5 callers. *Fix*: extraer builders junto a `tasteSignalLines` + un `loadStyleSignals(supabase, userId)`. *Por qué ahora no*: refactor de 9 archivos a mitad de un ship grande; cero cambio de comportamiento. *Trigger*: la próxima señal nueva que haya que cablear.
- [ ] **Backfill de taste_tags con la calibración √DF**: los ~18 perfiles existentes tienen tags ordenados con la fórmula vieja (n/DF) y los prompts ya anuncian "en orden de fuerza". Los swipes crudos viven en `events.data.swipes` — recomputar con `computeTasteTags` y escribir. *Trigger*: antes del experimento con usuarias nuevas no importa (onboarding nuevo ya usa √DF); hacerlo si los perfiles viejos siguen activos.
- [ ] **Mochila en frío: piso 3+2+2 satura el techo de 7** y no deja slot para la capa que el juez exige. *Fix candidato*: reservar 1 slot al comprimir cuando el clima pide abrigo, o relajar el piso si `piso == techo`. *Trigger*: primer `trip_capsule_underfloor` real con mochila, o viaje de mochila a clima frío en los logs.
- [ ] **Decisión de producto — restaurar looks rechazados por CLIMA**: la cobertura de ocasiones restaura looks que el juez rechazó por sub-abrigo irreparable (se muestran sin warning). Vetos ya NO se restauran (fix v0.2.0.0). *Opciones*: restaurar con warning de abrigo visible, o distinguir rechazo-clima. *Trigger*: quejas de looks fríos en viajes, o el experimento.

## Diferido del build (2026-07-04) — "Estílame como [referencia]"

- [ ] **Feature "estílame como [stylist/referencia]"**: dado el estilo de una referencia (Carla Figliozzi, María Zimmer, o en el futuro fotos que suba el usuario), generar looks NUEVOS en ese estilo y renderizarlos en el avatar del usuario. **CONCEPTO YA PROBADO** en R&D (script `scripts/gen-style-v2.mjs`, renders `docs_para_claude/outfit-inspo/CFZ/v2-look-*.png`). *Por qué ahora no*: es un feature nuevo grande con cero usuarios reales validando el flujo core; el aprendizaje ya se extrajo. *Trigger*: hay usuarios reales usando el motor de outfits Y piden inspiración más allá de su clóset.

  **Arquitectura validada (mapea a motores que la app ya tiene):**
  1. **Extraer estilo POR VISIÓN** (el 80% del resultado): el modelo VE las fotos reales de la referencia y extrae su *gramática de styling* (movimientos de firma + anti-reglas + clóset atrevido), NO un brief de texto sanitizado. Un brief escrito a mano ("neutros + un protagonista") es una mentira segura que produce looks genéricos. Reusa el patrón de `/api/analizar-prenda` pero multi-imagen.
  2. **Definir el CLÓSET** = motor de cápsula (`generateCapsuleTarget`).
  3. **Armar LOOKS** = motor de outfits, con restricciones Camino A (implementadas en el script): cada look usa ≥2 movimientos de firma, reparte los movimientos (ninguno se repite entre looks), varía el calzado, y mete novedad (aplica el movimiento sin copiar un outfit real de la referencia).
  4. Render en el avatar del usuario con el vibe v3 (grey-wall Gen-Z).

  **→ Camino B (el paso que falta para producción-calidad): JUEZ DE VISIÓN + NOVEDAD.** Un paso 4 que mira cada render y regenera si: (a) copió una foto real casi literal, (b) repitió el mismo movimiento/zapato entre looks, (c) el render tiró un elemento que el look pedía. Patrón de "crítico de completitud" (correr en `JUDGE_MODEL` = sonnet, no opus, para abaratar). *Por qué se difirió*: cuesta 2-3× llamadas por look y es pulido de calidad, no cambia la conclusión del experimento. Camino A (restricciones en el prompt de looks, sin llamadas extra) ya sube la calidad notablemente y fue lo que se implementó. Detalle operacional: las 17 fotos crudas dan 413 (request too large) — reducir con `sips -Z 900 -s format jpeg -s formatOptions 60` antes de mandarlas a visión.

## Diferido del deep research de prior art (2026-07-24)

Del informe de prior art (`docs_para_claude/compass_artifact_wf-a2162e9a-*.md`), revisado y verificado contra fuentes primarias. El motor salió mayormente alineado con el estado del arte (~12 de 14 reglas validadas). Ya aplicado en v26: EVITA near-face de veto→preferencia + acotar "camisa sobre suéter". Estas dos son instrumentación, NO se construyen hasta tener volumen de votos:

- [ ] **Correlación regla ↔ 👎 (tasa de disparo por regla)**: instrumentar cada regla del motor (EVITA near-face, "traje desparejado", "un solo estampado", "capas") con su tasa de disparo y su correlación con el voto 👎, para saber qué regla se gana su lugar en vez de discutirlo a ojo. *Por qué ahora no*: con <10% de tasa de voto y ~18 usuarias, la métrica mide ruido — decidir sobre 3 votos es el mismo pecado (folclore estadístico) que el research le acusa a la regla del "traje desparejado" (nació de 1 voto). Construir el velocímetro antes de tener qué medir. *Trigger*: N > ~100 votos reales (no perfiles — votos).
- [ ] **Test de consistencia del juez**: correr el mismo outfit dos veces por el crítico y verificar que el veredicto no baila (estabilidad) + medir position bias (¿favorece el primer outfit del batch?). Es autónomo — NO necesita usuarios, es un script de una tarde. *Por qué ahora no*: es QA de una pieza que ya funciona razonablemente; importante, no urgente. Justificado por el bug histórico de la rúbrica masculina por default (v23), que probó que el juez arrastra sesgos silenciosos. *Trigger*: cuando haya una tarde de QA, o antes de confiar veredictos del juez para decisiones automáticas (ej. #4b regenerar).

**Nota estratégica del research (no es un TODO, es el encuadre):** el reto de stailist NO es la calidad del outfit (validada-suficiente) — es retención + monetización, donde murieron Cladwell (~$330K ARR, no escaló) y Thread (ML puro, quebró → IP a M&S). Y el tapón inmediato sigue siendo el experimento con Tatiana/Toño: ninguna calibración del motor se valida sin votos reales.

## Diferido del módulo de evales (2026-08-07)

- [ ] **Redefinir "refrescar mi estilo"** — hoy es una etiqueta que NO cambia nada: `OCASION_EQUIVALENTE = { refrescar: "diario" }` en `lib/engine/inspiracion.ts` y sin piso de formalidad en `prompt.ts`, así que **refrescar y diario producen el mismo prompt carácter por carácter**. Lo único distinto es la frase del cargando ("una combinación distinta a la de siempre"), o sea que la app promete algo y llama al motor igual. *El razonamiento de Roberto que cierra el caso*: la no-repetición YA está en el prompt para TODAS las ocasiones (rotación v36), así que "distinto a ayer" no puede ser lo que refrescar significa — sería prometer lo que todos los botones ya hacen. *Su redefinición, que sí es algo que nada cubre*: **salir de la zona de confort de ESTILO** — "si te vistes prepi, algo edgy". Eso es una dimensión distinta de la rotación de prendas: es rotar la FAMILIA de estilo, no el inventario. *Por qué ahora no*: "hay cosas más importantes; lo dejaría hasta el final" (Roberto, 2026-08-07). *Trigger*: cuando el estilo (hoy 3.38 en el eval) suba y la pregunta pase a ser "¿cómo lo variamos?" en vez de "¿lo estamos honrando?". *Al reabrir*: decidir si es una ocasión o un modificador de cualquier ocasión — Roberto se inclinó por lo segundo ("lo pondría en todos").

- [ ] **La regla de cueros no distingue matiz en tonos oscuros** — el eval (corrida `7e10d12b`) cazó "cinturón café chocolate `#5C4433` + mocasines burdeos `#5C2A2E`" y la regla `cueros-que-no-se-hablan` NO disparó. La causa está medida: la distancia euclidiana RGB entre los dos es **26.5**, por debajo del umbral `MISMO_TONO = 60`, así que la regla los lee como el mismo café. Pero el **matiz** difiere 30° (25° contra 355°): uno es café y el otro es vino. La distancia RGB no separa matiz cuando los dos colores son oscuros y desaturados. El juez VISUAL sí lo vio ("el cinturón marrón chocolate desentona y arruina la armonía con los mocasines burdeos") — el juez de texto no. *Por qué ahora no*: bajar `MISMO_TONO` o sumar un check de matiz puede generar falsos positivos sobre looks ya aprobados, y una regla que dispara de más manda al juez a "reparar" lo que estaba bien. Misma disciplina que con café+negro+gris: **primero medir cuántos looks aprobados marcaría**. *Trigger*: que vuelva a aparecer en otra corrida del eval, o al hacer el barrido de cuántos looks afecta.

## Diferido de la review de ship (2026-08-12, v0.2.224.0 — ficha en chips)

- [ ] **El grid del clóset re-renderiza entero cada vez que abres una prenda** — `ClosetGrid` calcula `counts`, `grupos`, `presentes` y `eagerIds` como consts sin `useMemo` (7 pasadas de `filter` más un `flatMap` sobre hasta mil prendas), y `Tile` no está memoizado, así que `setSelected(p)` reconcilia las ~1000 tarjetas con su subárbol de `next/image`. La hoja en sí es barata —el rediseño la dejó con menos nodos que antes—; el techo está en el grid que la rodea. *Fix*: `useMemo` sobre los cuatro, `React.memo` en `Tile`, y estabilizar `onTap`/`onConjunto` con `useCallback` (hoy nacen nuevas en cada render, así que `React.memo` solo no serviría). *Por qué ahora no*: es un refactor del componente padre, ajeno al rediseño de la ficha, y con mil prendas hay que medirlo, no suponerlo. *Trigger*: el primer "se siente lento al abrir una prenda", o cuando un clóset real pase de ~300.

- [x] **La tercera copia del vocabulario de prendas** — RESUELTA 2026-08-14, borrando el archivo entero en vez de deduplicarlo. `components/add-photo-flow.tsx` era la puerta de alta de "una prenda": la misma función que el carrete pero peor, sin render limpio (dejaba la foto cruda en el clóset) y con un botón para pasarle la foto a la puerta de al lado cuando detectaba más de una prenda. Había producido **7 prendas de 1066** en toda la vida del producto. Con el archivo se fue su copia de `CATEGORIAS`/`FORMALIDADES`/`TEMPORADAS`, así que el vocabulario ya solo vive en `components/prenda-campos.tsx`.

- [ ] **`updateItemAttrs` reescribe el blob `attrs` entero** — lee, copia y vuelve a escribir todo el jsonb. El worker de render del propio clóset puede estar generando la imagen de la prenda que tienes abierta (es candidata justamente por no tener), y `/api/render-item` escribe `attrs.image_path`/`visual`: si aterriza entre la lectura y la escritura, la imagen recién generada —y pagada— se pierde del blob. *Fix*: update parcial (`attrs || jsonb_build_object(...)`) en vez de mandar el objeto completo. *Por qué ahora no*: toca la server action que usan todas las pantallas de edición y merece su propia verificación. *Trigger*: el primer reporte de "se me borró la imagen que acababa de generar", o el siguiente cambio a esa acción.

- [ ] **Las parejas del conjunto se ordenan por el color GUARDADO, no por el que estás editando** — `candidatas` memoiza sobre `item.swatch`: corriges el color del saco y las candidatas siguen ordenadas por el color viejo, en la misma pantalla. Además `todas` no lleva los renders locales, así que una candidata recién dibujada sale con caja vacía. *Por qué ahora no*: hay que decidir cuál es el ancla correcta —el color guardado (revertible) o el que estás escribiendo— y eso es una decisión de producto, no un bug. *Trigger*: la próxima vez que alguien ate un traje después de corregirle el color.

- [ ] **Guardar debería estar siempre a la mano** — con cambios pendientes el botón vive al final de la hoja, después del selector de conjunto; en un saco a 390px queda bajo el pliegue, y "cerrar" (el que pliega "+ más") es lo último que se ve. Esta versión tapó la pérdida de datos que eso causaba, pero el orden sigue siendo el equivocado. *Fix*: barra fija abajo con el guardar mientras haya pendientes. *Trigger*: la próxima pasada de diseño sobre la ficha.

## Diferido del rediseño del home (2026-08-11)

- [ ] **Alimentar el motor con lo que el fit check ve** — desde este rediseño el fit check escribe el evento `worn` (ahí murió la card que lo preguntaba), pero esas filas nacen con `item_ids: []` a propósito (el espejo NO empata la foto contra el clóset, ver `lib/espejo.ts`), así que `lib/engine/taste-signal.ts` las descarta: llegarían al prompt como *"SE LO PUSO de verdad: \<título\>"* sin una sola prenda y, por ser el cubo de más peso (6 lugares), desalojarían los recuerdos reales. Lo que SÍ tiene esa fila es `resumen`: lo que traía puesto, en palabras. Alimentar el motor con eso es la idea que queda pendiente. *Por qué ahora no*: es un cambio del motor, y aquí los cambios del motor se deciden midiendo (`/admin/comparador`, regla pre-registrada), no de oído — meterlo "porque suena bien" es exactamente lo que la casa no hace. *Trigger*: cuando haya volumen de fit checks (hoy es una función recién promovida) y una corrida pareada que compare con y sin el resumen.

- [x] **Reusar las fotos de destino en la sección Viaje** — **HECHO** (los handoffs de viaje de agosto llevaron las fotos a las dos vistas vía `fotosDeViajes` en `lib/destino-imagen-cache.ts`; en v0.2.235.0 la lista móvil las estrena como filas editoriales —foto a sangre de 96px, destino en serif— y el detalle las usa de portada que se recoge al cambiar de pestaña. Verificado 2026-08-13). ~~el set de 19 imágenes B&N (`public/destinos/`, elegidas por `lib/destino-imagen.ts`) hoy solo se ve en la card del home, que aparece **únicamente** cuando falta una semana o menos para el viaje: la mayor parte del año, cero. En `/viaje/lista` y `/viaje/[id]` se verían siempre.~~ *Trigger que disparó*: se abrió Viaje para el rediseño de pestañas.

- [ ] **La duplicación de la consulta de viajes** — `lib/home-trip.ts` y `lib/trip-context.ts` consultan la tabla `trips` por separado (la card necesita la maleta y sus faltantes; el badge de "Más" solo el destino y los días). Comparten la ventana y el cálculo de días —que ya viven en `lib/trip.ts`— pero no la consulta, así que un cambio de criterio (por ejemplo empezar a filtrar viajes cancelados) hay que hacerlo en dos lugares o el botón y la card empiezan a decir cosas distintas. *Por qué ahora no*: unificar obliga a que el badge pague el costo de leer la cápsula, que no necesita. *Trigger*: la tercera superficie que necesite el viaje vivo, o el primer bug de desincronía entre el badge y la card.
